/** localStorage key — current browser login session row id */
export const LOGIN_SESSION_STORAGE_KEY = 'crm-kai-login-session-id'

/** Heartbeat interval while app is open */
export const LOGIN_SESSION_TOUCH_MS = 5 * 60 * 1000
